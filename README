Jeu de Go via webservices

Pour lancer la compilation
    mvn compile
Pour lancer l'application
    mvn spring-boot:run
Pour lancer les tests
    mvn test

Ouvrez votre navigateur, et rendez-vous sur
    localhost:8080

Un bouton No server permet de s'abolir du webservice, pour débugger la partie Front (dans le navigateur), ou pour simplement jouer en local.